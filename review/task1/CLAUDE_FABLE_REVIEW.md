Reviewing the packet content only, as instructed. No tools run, nothing edited.

## Findings

| ID | Severity | Location | Trigger and evidence | Impact | Minimal correction | What would falsify this |
|---|---|---|---|---|---|---|
| C1 | QUESTION | `review/task1/protected-before.json`; `verify_task1.py` `governance()` | The 28-file baseline was produced by the same actor that did the task, and HEAD tracks only README.md. The internal chain (Constitution self-hash, manifest bindings, protocol bindings, 14 sidecars) all verifies, which rules out accidental alteration such as CRLF or whitespace stripping. It does not rule out a consistent wholesale regeneration before the snapshot was taken. | The "unchanged frozen files" claim rests on a self-generated anchor. | Owner compares the Constitution content hash `4cb6c7d3…` and `FROZEN_HASHES.json.sha256` value `962bdb50…` against their own out-of-repo record from the 2026-09-11 freeze. No repo change. | Owner confirms both values match their independent record. |
| C2 | NON-BLOCKING | `README.md`, "Local development" section, last two paragraphs | README documents a machine-specific, gitignored portable `.venv` that no clone will have, and lists `git diff --check` under "same checks as CI" although CI does not run it. The documented fresh-install path (`python -m venv` then `pip install -e ".[dev]"`) was never executed as written; the local install needed `--no-build-isolation`. | Spec asked for a concise README; a new contributor gets instructions that cannot apply to their machine and an inaccurate CI description. | Move the portable-runtime notes to `review/task1/`, and reword the command list as "checks CI runs, plus `git diff --check`". Verify the install path once on standard CPython or via the first CI run. | A fresh standard CPython 3.12 venv installs as written and the owner wants the portable note kept. |
| C3 | NON-BLOCKING | `review/task1/run_checks.ps1`; `verify_task1.py` imports `yaml`, `jsonschema` | The evidence suite needs `jsonschema` (installed ad hoc) and PyYAML (only transitively via pre-commit). Neither is declared anywhere, so `run_checks.ps1` fails on a clean `.[dev]` install. `boundary-probes.json` also embeds absolute local paths including the short-name user profile directory. | Evidence is not reproducible from the documented setup. Path leakage is not a secret under SECURITY.md, just hygiene. | A one-line note in `review/task1/` stating the extra install, or an optional `review` extra. Optionally drop `cwd` and `command[0]` from the probe JSON. | A clean `.[dev]` install runs `run_checks.ps1` to completion. |
| C4 | QUESTION | `pyproject.toml`, contracts "Governor cannot reach execution or model training" and "Execution cannot reach model training" | Spec says "model-training/research modules". The contracts map that to `aqt.models` plus `aqt.research`. `aqt.features`, `aqt.validation`, `aqt.backtest`, and `aqt.benchmarks` are not forbidden for governor or execution. Spec says "at minimum", so this is not a defect. | The definition needs settling before those packages gain code, or the governor could legitimately import the validation engine. | Record the intended definition now; extend `forbidden_modules` when the first of those packages is populated. | Owner states that "model-training/research" means exactly `models` and `research`. |
| C5 | NON-BLOCKING | `.github/workflows/ci.yml`; GitHub branch settings | Workflow has never executed. Constitution §17 says "CI enforces", but a failing workflow blocks nothing unless branch protection requires the `checks` job on `main`. That setting lives outside the repo and is not evidenced. Also, `cache: pip` with only `pyproject.toml` should work on setup-python v5 but is unverified here. | Import boundaries and lint gates are advisory until a run passes and the check is required. | On the first authorized push, observe a green run and enable the required status check. If the cache step errors on missing dependency files, drop `cache: pip` or set `cache-dependency-path`. | A passing run on `main` plus a screenshot or settings export showing the required check. |

No blocker found. The ruff pin matches between pyproject and pre-commit. The `!.env.example` negation is correct and was tested. The exclusion regex was checked against all 28 paths. The probe runner now calls the real click entry point and all 20 violations exit 1 against the expected contract with the expected file counts (18 direct, 19 indirect), which shows the temporary copy was the tree analysed. `.env.example` and SECURITY.md contain no secrets. The environment freeze exposes only the public GitHub remote.

## Shared assumptions and missing evidence

- **`git diff --check` proved nothing.** Only README.md is tracked, so the required acceptance step was vacuous for every new file. A reversible check is `git add --intent-to-add -A`, then `git diff --check`, then `git reset`. Expect it to flag the frozen Constitution, because its Markdown hard line breaks are intentional trailing double spaces. That is the reason the pre-commit exclusion is load-bearing rather than cosmetic, and it means no future whitespace gate may include frozen paths.
- **pre-commit hooks never ran.** Exclusion was verified by regex simulation in Python `re.search`, which matches pre-commit's own matcher, but `pre-commit run --all-files` was not executed. It needs network to fetch hook repos.
- **All validation evidence is from Windows.** CI is Linux. I see no case-sensitivity or path issue in the packet, but nothing has confirmed it.
- **Import-linter is static.** `importlib.import_module`, string-based plugin loading, subprocesses, and anything outside `aqt` (`scripts/`, `experiments/`, `tests/`) are invisible to all four contracts. A future predictor or live package must be added to the live-path contract; README states this, CI cannot enforce it.
- **Version is stated twice.** `pyproject.toml` and `src/aqt/core/version.py` both say 0.1.0 with no drift check. Acceptable for Task 1.

## Verdict

**TASK 1 PASS.** Every original acceptance criterion maps to a file and to a passing check, frozen bytes verify through the full hash chain and through Git filters, the boundary contracts are proven to fail on all 20 required edges, and no business, scientific, or exchange logic exists. The open items are evidence gaps to close on first push (C5), documentation and reproducibility hygiene (C2, C3), and two owner decisions (C1, C4). This is an external review, not approval to commit, merge, or trade.
